[project]
name = "webpage"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
dependencies = [
    "database-io",
]

[tool.uv.sources]
database-io = { workspace = true }
